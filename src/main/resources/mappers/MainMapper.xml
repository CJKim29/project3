<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.githrd.project3.dao.MainMapper">

    <select id="getRandomPerformancesByGenre" parameterType="map" resultType="Performance">
    SELECT * FROM performance
    WHERE performance_detail_cate_idx IN (
        SELECT performance_detail_cate_idx 
        FROM performance_detail_cate 
        WHERE performance_detail_cate_name = #{genre}
    )
    ORDER BY RAND()
    LIMIT #{limit}
</select>

</mapper>
